API:
users | Set

access_tokens.{username} | Hash
  {access_token}: expiry_date

{username}.refresh_token | String

seenPosts.{username}.{tag}.{startingTimePeriod} | Set | expiry time 7 days

presets.{username} | Hash

preset.{username}.{preset} | Set

savedPosts.{username} | List